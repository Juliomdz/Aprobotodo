import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Aprobotodo.app',
  appName: 'Aprobotodo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
